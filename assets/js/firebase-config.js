(function(){
  // Optional static Firebase Web config.
  // Leave as null to configure through My account -> Backend connection -> Save config.
  // You can also set this object in deployed environments to avoid localStorage config setup.
  window.__FIREBASE_CONFIG__ = window.__FIREBASE_CONFIG__ || null;
})();
